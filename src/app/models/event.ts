export interface EventDTO {
  eventId: number,
  eventDate: string,
  title: string,
  description?: string,
  id: string
}

export interface TeamEvent {
  eventId: number,
  eventDate: string,
  title: string,
  description: string,
  id: string
}
